{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}

    <h1>You can add a fucking category, here, my nigger !!!</h1>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
    <br>
    <a href="{{ path('homepage') }}">
        Back to homepage.
    </a>

{% endblock %}
